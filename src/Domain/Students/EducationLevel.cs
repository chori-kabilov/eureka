namespace Domain.Students;

// Уровень образования
public enum EducationLevel
{
    Preschool = 0,     // Дошкольник
    Elementary = 1,    // Начальная школа (1-4 класс)
    MiddleSchool = 2,  // Средняя школа (5-9 класс)
    HighSchool = 3,    // Старшая школа (10-11 класс)
    University = 4,    // Студент ВУЗа
    Graduate = 5,      // Выпускник ВУЗа
    Working = 6,       // Работающий
    Other = 7          // Другое
}
