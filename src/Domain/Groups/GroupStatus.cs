namespace Domain.Groups;

// Статус группы
public enum GroupStatus
{
    Draft = 0,      // Черновик
    Active = 1,     // Активная
    Completed = 2,  // Завершена
    Cancelled = 3   // Отменена
}
