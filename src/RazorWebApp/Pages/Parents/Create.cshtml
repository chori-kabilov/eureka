@page
@model RazorWebApp.Pages.Parents.CreateModel
@{
    ViewData["Title"] = "Добавить родителя";
}

<div class="container">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card shadow">
                <div class="card-header">
                    <h5 class="mb-0"><i class="bi bi-person-plus me-2"></i>Добавить родителя</h5>
                </div>
                <div class="card-body">
                    @if (!string.IsNullOrEmpty(Model.ErrorMessage))
                    {
                        <div class="alert alert-danger">@Model.ErrorMessage</div>
                    }

                    <form method="post">
                        <div class="mb-3">
                            <label class="form-label">Пользователь</label>
                            <select asp-for="UserId" class="form-select" required>
                                <option value="">Выберите пользователя...</option>
                                @foreach (var user in Model.AvailableUsers)
                                {
                                    <option value="@user.Id">@user.FullName (@user.Phone)</option>
                                }
                            </select>
                            <div class="form-text">Выберите пользователя, который станет родителем</div>
                        </div>

                        <div class="d-flex justify-content-between">
                            <a asp-page="/Parents/Index" class="btn btn-outline-secondary">
                                <i class="bi bi-arrow-left"></i> Назад
                            </a>
                            <button type="submit" class="btn btn-primary">
                                <i class="bi bi-check-lg"></i> Создать
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>
