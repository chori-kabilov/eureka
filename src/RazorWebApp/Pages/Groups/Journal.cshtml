@page "{id:guid}"
@model RazorWebApp.Pages.Groups.JournalModel
@{
    ViewData["Title"] = "Журнал - " + (Model.Group?.Name ?? "Группа");
}

<div class="container-fluid">
    @if (Model.Group != null)
    {
        <div class="d-flex justify-content-between align-items-center mb-4">
            <div>
                <h2><i class="bi bi-journal-text me-2"></i>Журнал</h2>
                <div class="text-muted">@Model.Group.Name | @Model.Group.CourseName</div>
            </div>
            <a asp-page="/Groups/Details" asp-route-id="@Model.Group.Id" class="btn btn-outline-secondary">
                <i class="bi bi-arrow-left"></i> Назад к группе
            </a>
        </div>

        <!-- Легенда -->
        <div class="mb-3">
            <span class="badge bg-success me-2">✅ Был</span>
            <span class="badge bg-danger me-2">❌ Не был</span>
            <span class="badge bg-warning text-dark me-2">⚠️ Опоздал</span>
            <span class="badge bg-info me-2">📋 Уважит.</span>
        </div>

        <!-- Журнал -->
        <div class="card">
            <div class="card-body p-0">
                <div class="table-responsive">
                    <table class="table table-bordered table-sm mb-0">
                        <thead class="table-light">
                            <tr>
                                <th class="sticky-col" style="min-width:200px;">Ученик</th>
                                @foreach (var lesson in Model.Lessons)
                                {
                                    <th class="text-center" style="min-width:60px;">
                                        <div>@lesson.Date.ToString("dd.MM")</div>
                                        <small class="text-muted">
                                            @(lesson.Type == 1 ? "Экз" : "Урок")
                                        </small>
                                    </th>
                                }
                                <th class="text-center" style="min-width:80px;">Ср. балл</th>
                                <th class="text-center" style="min-width:60px;">%</th>
                            </tr>
                        </thead>
                        <tbody>
                            @if (Model.Journal.Any())
                            {
                                @foreach (var row in Model.Journal)
                                {
                                    <tr>
                                        <td class="sticky-col fw-medium">
                                            @row.StudentName
                                            @if (row.IsChild)
                                            {
                                                <i class="bi bi-person-fill text-info" title="Ребёнок"></i>
                                            }
                                        </td>
                                        @foreach (var cell in row.Cells)
                                        {
                                            <td class="text-center">
                                                @if (cell.AttendanceStatus.HasValue)
                                                {
                                                    @switch (cell.AttendanceStatus.Value)
                                                    {
                                                        case 0:
                                                            <span class="text-success">✅</span>
                                                            break;
                                                        case 1:
                                                            <span class="text-danger">❌</span>
                                                            break;
                                                        case 2:
                                                            <span class="text-warning">⚠️</span>
                                                            break;
                                                        case 3:
                                                            <span class="text-info">📋</span>
                                                            break;
                                                        default:
                                                            <span>—</span>
                                                            break;
                                                    }
                                                }
                                                else
                                                {
                                                    <span class="text-muted">—</span>
                                                }
                                                @if (cell.Grade.HasValue)
                                                {
                                                    <div class="small fw-bold">@cell.Grade</div>
                                                }
                                            </td>
                                        }
                                        <td class="text-center fw-bold">
                                            @(row.AverageGrade?.ToString("0.0") ?? "—")
                                        </td>
                                        <td class="text-center">
                                            <span class="badge @(row.AttendancePercent >= 80 ? "bg-success" : row.AttendancePercent >= 50 ? "bg-warning text-dark" : "bg-danger")">
                                                @row.AttendancePercent%
                                            </span>
                                        </td>
                                    </tr>
                                }
                            }
                            else
                            {
                                <tr>
                                    <td colspan="@(Model.Lessons.Count + 3)" class="text-center text-muted py-4">
                                        <i class="bi bi-journal-x fs-1 d-block mb-2"></i>
                                        Нет данных для отображения
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    }
    else
    {
        <div class="alert alert-warning">Группа не найдена</div>
    }
</div>

<style>
    .sticky-col {
        position: sticky;
        left: 0;
        background: white;
        z-index: 1;
    }
</style>
